# Graph artifacts

Small reproducible summaries from local Neo4j graph construction and verification are stored here.
The Neo4j database itself is persisted in a Docker volume and is not committed to Git.
